export interface Club {
    id: number
    name: string
    contact_person: string
    email: string
    phone: string
    address: string
    website: string
    created_at: string
    image_url: string
  }